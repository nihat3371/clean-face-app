specVersion: "v1"
serviceId: "clean-face-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "clean-face-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
